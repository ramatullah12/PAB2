name: nfc_reader_yuan
description: "A new Flutter project to read NFC cards."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2           # Untuk format tanggal dan waktu
  nfc_manager: ^3.5.0     # Library utama untuk membaca NFC

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
